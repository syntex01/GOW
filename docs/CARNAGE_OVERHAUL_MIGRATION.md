# Carnage overhaul migration

This temporary marker keeps the implementation pull request visible while the self-verifying migration applies the persistent Carnage roster, research-tree layout, inherited slot buffs, and deployable Incarnation of Slaughter.

The migration removes its own bootstrap workflow after `npm run check` succeeds.
